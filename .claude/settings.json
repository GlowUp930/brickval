{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_TOOL_INPUT\" | grep -q '\\.env' && echo 'BLOCKED: Do not edit .env files — they contain live production credentials.' && exit 1 || exit 0"
          }
        ]
      }
    ]
  }
}
